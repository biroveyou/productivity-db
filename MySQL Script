-- MySQL Script gerado no MySQL Workbench
-- Abr 25 15:54:41 2026
-- Modelo: Novo Model    Version: 1.0
-- MySQL Workbench Forward Engineering

-- Configurações iniciais
SET @OLD_UNIQUE_CHECKS=@@UNIQUE_CHECKS, UNIQUE_CHECKS=0;
SET @OLD_FOREIGN_KEY_CHECKS=@@FOREIGN_KEY_CHECKS, FOREIGN_KEY_CHECKS=0;
SET @OLD_SQL_MODE=@@SQL_MODE, 
SQL_MODE='ONLY_FULL_GROUP_BY,STRICT_TRANS_TABLES,NO_ZERO_IN_DATE,NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_ENGINE_SUBSTITUTION';

-- Criação do schema
CREATE SCHEMA IF NOT EXISTS `mydb` DEFAULT CHARACTER SET utf8mb4;
USE `mydb`;

-- -----------------------------------------------------
-- Tabela Usuario
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `Usuario` (
  `id_usuario` INT NOT NULL AUTO_INCREMENT,
  `nome` VARCHAR(45) NOT NULL,
  `email` VARCHAR(60) NOT NULL,
  `data_criacao` DATETIME DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id_usuario`),
  UNIQUE (`email`)
) ENGINE=InnoDB;

-- -----------------------------------------------------
-- Tabela Problema
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `Problema` (
  `id_problema` INT NOT NULL AUTO_INCREMENT,
  `nome` VARCHAR(45) NOT NULL,
  `descricao` VARCHAR(200),
  `gravidade` VARCHAR(20),
  PRIMARY KEY (`id_problema`)
) ENGINE=InnoDB;

-- -----------------------------------------------------
-- Tabela RegistroDiario
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `RegistroDiario` (
  `id_registro` INT NOT NULL AUTO_INCREMENT,
  `data` DATE NOT NULL,
  `humor` VARCHAR(45),
  `produtividade` INT, -- escala numérica (ex: 1 a 5)
  `observacoes` VARCHAR(200),
  `id_problema` INT NULL,
  `id_usuario` INT NOT NULL,
  PRIMARY KEY (`id_registro`),

  CONSTRAINT `fk_registro_usuario`
    FOREIGN KEY (`id_usuario`)
    REFERENCES `Usuario` (`id_usuario`)
    ON DELETE CASCADE
    ON UPDATE CASCADE,

  CONSTRAINT `fk_registro_problema`
    FOREIGN KEY (`id_problema`)
    REFERENCES `Problema` (`id_problema`)
    ON DELETE SET NULL
    ON UPDATE CASCADE
) ENGINE=InnoDB;

-- -----------------------------------------------------
-- Tabela Estudo
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `Estudo` (
  `id_estudo` INT NOT NULL AUTO_INCREMENT,
  `duracao_horas` DECIMAL(4,2) NOT NULL,
  `descricao` VARCHAR(200),
  `id_registro` INT NOT NULL,
  PRIMARY KEY (`id_estudo`),

  CONSTRAINT `fk_estudo_registro`
    FOREIGN KEY (`id_registro`)
    REFERENCES `RegistroDiario` (`id_registro`)
    ON DELETE CASCADE
    ON UPDATE CASCADE
) ENGINE=InnoDB;

-- -----------------------------------------------------
-- Tabela Topico
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `Topico` (
  `id_topico` INT NOT NULL AUTO_INCREMENT,
  `nome` VARCHAR(45) NOT NULL,
  `descricao` VARCHAR(200),
  PRIMARY KEY (`id_topico`)
) ENGINE=InnoDB;

-- -----------------------------------------------------
-- Tabela EstudoTopico (N:N)
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `EstudoTopico` (
  `id_estudo` INT NOT NULL,
  `id_topico` INT NOT NULL,

  PRIMARY KEY (`id_estudo`, `id_topico`),

  CONSTRAINT `fk_estudo_topico_estudo`
    FOREIGN KEY (`id_estudo`)
    REFERENCES `Estudo` (`id_estudo`)
    ON DELETE CASCADE
    ON UPDATE CASCADE,

  CONSTRAINT `fk_estudo_topico_topico`
    FOREIGN KEY (`id_topico`)
    REFERENCES `Topico` (`id_topico`)
    ON DELETE CASCADE
    ON UPDATE CASCADE
) ENGINE=InnoDB;

-- Restaurando configurações
SET SQL_MODE=@OLD_SQL_MODE;
SET FOREIGN_KEY_CHECKS=@OLD_FOREIGN_KEY_CHECKS;
SET UNIQUE_CHECKS=@OLD_UNIQUE_CHECKS;
